Translate the following text into {{ target_language }}.

Preserve Markdown formatting (headings, lists, emphasis, code fences) and
any LaTeX math (\( ... \), \[ ... \]) exactly as written — translate only
the surrounding prose, never the markup or the math itself. Respond with
ONLY the translated text: no preamble, no commentary, no fences around
your answer.

---

{{ text }}
